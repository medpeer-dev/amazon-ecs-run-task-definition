{
  "name": "medpeer-amazon-ecs-run-task-definition",
  "version": "1.0.0",
  "description": "Run an Amazon ECS task definition on the same configuration as the existing ECS service.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint **.js",
    "package": "ncc build index.js -o dist",
    "test": "eslint **.js && jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/medpeer-dev/amazon-ecs-run-task-definition.git"
  },
  "keywords": [
    "AWS",
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "MedPeer",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/medpeer-dev/amazon-ecs-run-task-definition/issues"
  },
  "homepage": "https://github.com/medpeer-dev/amazon-ecs-run-task-definition#readme",
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@aws-sdk/client-ecs": "^3.987.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.3",
    "eslint": "^7.7.0",
    "jest": "^29.7.0"
  }
}
